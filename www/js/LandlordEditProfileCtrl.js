angular.module('rentee.controllers')
  .controller('LandlordEditProfileCtrl', function($scope, $auth) {
    /* $scope.handleSaveBtnClick = function(form) {

      How to update if not null?
      $auth.submitRegistration(
      {
        f_name:                 form.f_name,
        l_name:                 form.l_name,
        email:                  form.email,
        password:               form.password,
        password_confirmation:  form.password_confirmation
      }, {
        config: 'landlord'
      })
        .then(function(resp) {
          // handle success response
          console.log("yay");
        })
        .catch(function(resp) {
          // handle error response
          console.log("nay");
        });
*/
    
  });
